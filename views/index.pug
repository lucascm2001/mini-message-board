extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href = '/new') Create New Message
  
  for message in allMessages
    ul
      li= `Text: ${message.text}`
      li= `User: ${message.user}` 
      li= message.added
  